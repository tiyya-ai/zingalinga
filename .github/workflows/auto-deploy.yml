name: 🚀 Auto Deploy to VPS

on:
  push:
    branches: [ main, master ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Get Code
      uses: actions/checkout@v4

    - name: 🔧 Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: 📦 Install & Build
      run: |
        npm ci
        npm run build

    - name: SSH and deploy
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: 109.199.106.28
        username: root
        password: Secureweb25
        script: |
          echo "🛑 Stopping Apache to free port 80..."
          systemctl stop apache2
          systemctl disable apache2
          
          echo "🚀 Deploying Next.js app..."
          cd /var/www/zinga-linga || mkdir -p /var/www/zinga-linga && cd /var/www/zinga-linga
          git pull origin main || git clone https://github.com/${{ github.repository }} .
          npm install
          npm run build
          
          echo "🔄 Starting Next.js on port 80..."
          pm2 stop zinga-linga || echo "No process to stop"
          pm2 delete zinga-linga || echo "No process to delete"
          pm2 start npm --name "zinga-linga" -- start -- --port 80
          pm2 save
          
          echo "✅ Next.js app running on port 80! Check zingalinga.io"